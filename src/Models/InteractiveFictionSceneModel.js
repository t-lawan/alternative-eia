export class InteractiveFictionSceneModel {
    order;
    general_text;
    left;
    right;
  
    constructor(order, general_text, left, right) {
            this.order = order;
            this.general_text = general_text;
            this.left = left;
            this.right = right;
    }
  }
  